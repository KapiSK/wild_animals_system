# Calendar View Task List

- [x] カレンダー用CSSの追加
  - [x] グリッドレイアウト、月切り替えボタン、日付マスのスタイル定義
  - [x] 選択状態（アクティブ）のスタイル定義
- [x] HTML要素の追加
  - [x] 上部タブメニューに `Calendar` を追加
  - [x] `gallery-calendar` コンテナ（非表示状態）を追加
- [x] カレンダー生成ロジック(JS)の追加
  - [x] 状態管理変数 `currentCalendarMonth` (Dateオブジェクト) などの追加
  - [x] メタデータの `cycle_time` をパースして、年月ごとにサイクルをグループ化する関数
  - [x] `renderCalendar()` 関数の実装（月間カレンダーを描画し、動物・人のアイコンと件数バッジをマスに埋め込む）
- [x] 日別詳細展開ロジック(JS)の追加
  - [x] カレンダーの日付をクリックした時に呼ばれる `showDateEvents(dateString)` の実装
  - [x] 選択された日のサイクルリストを、Flatモードと同じUI（動画付き・ページネーション付き）でカレンダー下部に描画する処理
- [x] `fetchImages` の完了後にカレンダーも再描画するフックの追加
- [x] 動作確認
