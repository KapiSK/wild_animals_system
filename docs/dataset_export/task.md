# データセット一括ダウンロード機能 実装タスク

- [x] 1. **バックエンド: BB情報の保存**
  - `server.py` の `analyze_image_for_cycle` を修正し、正規化座標と信頼度を返すようにする。
  - `process_cycle` で受け取り、`event_metadata` の画像情報に `detections` を保存するよう改修。

- [x] 2. **バックエンド: エクスポートプレビューAPI (`/api/export/preview`) の実装**
  - 条件（カメラ、期間、ラベル、確信度）に応じたフィルタリング処理。
  - 合致するメタデータの総数とIDリストを返す。

- [x] 3. **バックエンド: エクスポートダウンロードAPI (`/api/export/download`) の実装**
  - ページネーション（`page` パラメータ、1ページ最大1000件）対応。
  - MegaDetectorフォーマット (`md_results.json`) への変換。
  - ZIPアーカイブの動的生成と `FileResponse` による返却。
  - 過去データ（BBなし）のフォールバック対応。

- [x] 4. **フロントエンド: モーダルUIの追加 (HTML/CSS)**
  - 統計画面 (`/statistics`) 上部に「📦 Export Dataset」ボタンを配置。
  - カメラ、期間、対象ラベル、確信度の入力フォーム。

- [x] 5. **フロントエンド: モーダルのロジック実装 (JS)**
  - `preview` API を叩いて件数を確認するロジック。
  - 上限（1000件）超過時に、分割ダウンロード用ボタンを動的生成するロジック。
  - ZIPダウンロード中のローディング表示。

- [x] 6. **動作確認**
  - テスト画像の推論でBBが保存されるか確認。
  - モーダルからのZIP生成とダウンロードが成功するか確認。

- [x] 7. **ウォークスルーの作成**
  - `walkthrough.md` を作成して変更内容をまとめる。
