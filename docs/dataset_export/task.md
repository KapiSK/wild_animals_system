# タスクリスト: データセットエクスポート機能の再構築

- [x] `server.py`: `/api/export/download` のZIP生成処理に動画ファイル(`.mp4`)の包含処理を追加
- [x] `server.py`: ギャラリー (`/gallery`) のヘッダーUIに「Export Dataset」ボタンを追加
- [x] `server.py`: ギャラリー画面にエクスポート設定用モーダル (HTML/CSS) を追加
- [x] `server.py`: ギャラリー画面にエクスポート実行用 JavaScript ロジックを追加
- [x] 動作検証: ブラウザからのエクスポートモーダルの表示、プレビュー取得、ダウンロード実行の確認（ローカルでの実行確認を前提）
