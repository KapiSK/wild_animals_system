# Implementation Plan - 指導教員指摘事項の反映

指導教員（AI）からの指摘に基づき、論文の構成と内容を強化します。

## User Review Required

> [!IMPORTANT]
> **数値データのプレースホルダー**
> Abstractや結論に「XX%削減」といった具体的な数値のプレースホルダーを追加します。これらは後ほど実験データが揃い次第、実数値に置換する必要があります。

## Proposed Changes

### docs/ieice_paper_draft

#### [MODIFY] [main.tex](file:///c:/Users/kapib/vscodegit/wild_animals/test2/docs/ieice_paper_draft/main.tex)

1. **Abstract (あらまし)**
    - 低コスト性だけでなく、「階層的なフィルタリングによる通信帯域の最適化」を強調。
    - 「誤検知をXX%削減し...」といった具体的な成果の記述テンプレートを追加。

2. **3. 提案システム**
    - 冒頭の導入文をリライト。
    - **重複の削除**: 「システム構成」と重複する具体的なハードウェア説明を導入部から削減。
    - **設計思想の追加**: なぜ「エッジ側でフィルタリングが必要なのか（通信コストと電力の制約）」を明記。
    - **未完成行の削除**: Line 99付近の `フィルタリング後の画像は` を削除。
    - **句読点修正**: 前回のタスクで提案した `、` `。` -> `，` `．` の修正も同時に適用。

3. **4. 実験と評価**
    - **4.3 エッジサーバの評価**: 「見逃し（False Negative）」に関する考察パラグラフを追加。
    - **4.5 総合評価**: 「既存システムとの比較」サブセクションを追加。市販LTEカメラとのコスト・運用性の比較議論を挿入。

## Verification Plan

### Manual Verification

- `view_file` で修正箇所を確認。
- 特に削除した未完成行が綺麗に消えているか、文脈が繋がっているかを確認。
- `platex` は現在環境起因で失敗する可能性があるため、ソースコードレベルでの論理チェックを優先します。
