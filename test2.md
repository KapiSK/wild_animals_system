# wild_animals プロジェクト・アーキテクチャ定義

今後のAIとのやり取りおよびコーディングにおいて、以下のディレクトリ（ノード）と役割分担を**前提知識**として厳守すること。「エッジ」と「統合」をはじめとする各ノードの呼称と目的を混同しないようにする。

## ノードおよびディレクトリ構成

1. **`esp` / 自作カメラ (ESP32)**
   - **役割**: 野生動物などを現地で撮影し、画像データをエッジサーバー（pi）へ送信するセンサーノード。

2. **`pi` / エッジサーバー (Raspberry Pi)**
   - **役割**: ESP32からの画像をローカルで受信し、一次処理（YOLO等による推論）を行い、動物や人間の検出があればクラウドサーバー（original_server）へ転送する中間ノード。

3. **`satos` / 統合サーバー (Integration Server)**
   - **役割**: 自作カメラではない、既製品のカメラ等から指定のGmailアカウントに送られてきた動画をポーリング取得し、フレーム抽出や推論の一次処理を行って、クラウドサーバーへ転送するノード。

4. **`original_server` / クラウドサーバー (Cloud Server)**
   - **役割**: 全てのエッジ（pi）および統合（satos）サーバーからのアップロードデータを集約し、最終的な分析・画像結合・管理者宛のメール通知（アラート送信）などを統合管理するメインノード（FastAPI）。

## コーディングの際の重要指針

- **インターフェースの整合性**: エッジ側（pi, satos）のスクリプトを改修する際は、必ずクラウド側（original_server）のAPIエンドポイントやファイル命名等のパース仕様（正規表現など）と矛盾がないかを確認すること。
- **機能分離の原則**: エッジで行うべき前処理と、クラウドで行うべき集約・通知機能の責務を区別し、処理が片方に偏らないようにすること。
