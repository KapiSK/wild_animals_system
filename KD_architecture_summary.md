# KD 野生動物監視システム アーキテクチャ概要

本ドキュメントでは、`test2` プロジェクトにおける KD エッジカメラ、エッジサーバ、クラウドサーバの詳細なアーキテクチャと各ノードの役割、通信方式、および制御ロジックについてまとめます。

## 1. KD エッジカメラ (KD_edge_cam)
**役割**: 野生動物などを現地で撮影し、画像データをエッジサーバへ送信する超低消費電力のセンサーノード。

### 実装機能と制御ロジック
- **PIC マイコン (電源管理 - `KD_edge_cam/pic/main.c`)**:
  - **超低消費電力待機**: 待機時はESP32側の電源（MOSFET）を完全に遮断（0W待機）。PIR（人感）センサーからの割り込み検知でのみスリープから復帰します。
  - **電源供給とタイムアウト保護**: 検知時にMOSFETをONにし、ESP32をコールドブートさせます。ESP32から「処理完了（PIN_DONE）」信号を受け取るか、タイムアウト（3分間）に達すると、MOSFETをOFFにし、WDT（ウォッチドッグタイマ）を利用した一定のインターバル（約3.5分）スリープへ移行します（連続撮影の防止）。
- **ESP32 カメラモジュール (デュアルCPU構成 - `KD_edge_cam/esp/camera_s3_plus/`)**:
  - **撮影シーケンス**: PICからの給電で起動（コールドブート）し、直ちに4枚連続撮影を行います。1枚目は露出安定化のため破棄し、残り3枚をSDカードに保存します。
  - **再送機能**: アップロード失敗時のリトライ機能や、未送信の過去サイクル（直近3件など）を再送するロジックが組み込まれています。
  - **シャットダウン要求**: 処理完了後、PICに対して `PIN_DONE` をHIGHにして電源切断を要求します。

### 通信方式
- **ネットワーク探索**: mDNS（例: `kd01-rp4.local`）を利用して、動的にエッジサーバ（Raspberry Pi）のローカルIPアドレスを解決します。
- **データ送信**: Wi-Fi (STAモード) に接続後、HTTP POST リクエストで画像を送信します。
  - **ヘッダー情報**: `X-API-KEY` (認証), `X-Cycle-Id` (サイクル識別), `X-Content-SHA256` (データ破損検知) を付与します。

## 2. エッジサーバ (KD_pi_server / pi)
**役割**: エッジカメラからの画像をローカルネットワーク内で受信し、一次推論処理（YOLO）を行って、動物や人の検出があった場合のみクラウドサーバへデータ転送を行う中間ノード。

### 実装機能と制御ロジック
- **FastAPI 非同期レシーバー (`main.py`)**:
  - ESP32からの HTTP POST (`/upload` および `/esp_log`) を高速かつ非同期に受信し、ディスクへ保存して即座に `200 OK` を返却します（ESP32の電源稼働時間を最小化するため）。
- **CycleManager (サイクル管理とバッファリング)**:
  - 1回の撮影イベント（3枚の連続画像）を `Cycle ID` に基づき一つの「サイクル」としてメモリ上で管理します。
- **YOLO オブジェクト検出 (`detector.py`)**:
  - バックグラウンドで順次YOLO推論を実行。3枚の画像のうち1枚でもターゲット（動物や人）が検出された場合のみ、クラウドサーバへの転送をトリガーします。
  - これにより、風で揺れる草木などの誤検知画像をエッジ側で破棄し、クラウド側の負荷と通信帯域を大幅に節約します。
- **ローカルダッシュボード**:
  - `/gallery` エンドポイントにて、設置時の画角確認や動作チェックに使える軽量なHTMLベースのUIを提供します。

### 通信方式
- **受信用**: ESP32からローカルWi-Fi経由で HTTP POST を受け付けます。APIトークン(`API_TOKEN`)によるアクセス制御を実施。
- **クラウドへの転送**: ターゲット検出時、`httpx` を使用してクラウドサーバ (`MAIN_SERVER_URL`) のエンドポイントへ HTTP POST で画像を転送します。

## 3. クラウドサーバ (original_server)
**役割**: 全てのエッジサーバ（`pi`）および統合サーバ（`satos`）からのアップロードデータをインターネット経由で集約し、最終的な分析、保存、Web UI提供、管理者宛のアラート（メール通知）などを統合管理するメインノード。

### 実装機能と制御ロジック
- **FastAPI 統合バックエンド (`server.py`)**:
  - 複数のエッジサーバからの画像アップロードを受け付け、`received_images` および `processed_images` として管理。
- **高度なデータ管理とメタデータ生成**:
  - エッジ側で付与された `Cycle ID` を基にイベントを紐付け、`event_metadata`（JSON形式）を生成・更新します。
  - クラウド側のリッチなリソースを活用した高精度のYOLOv5推論（`yolov5` ディレクトリ）を用いて対象物の詳細なラベル付けとカウントを実施。
  - サーバ側で一意のシーケンス番号（`server_sequence.json`）を割り当て、重複管理や時系列管理を行います。
- **ユーザー・権限管理**:
  - `user_access_config.json` を基盤とした Basic認証とセッションCookieによるセキュリティ。
  - 管理者（Admin: 全機能アクセス可）と一般ユーザー（許可されたカメラの画像のみ閲覧可能）のロールベースのアクセス制御。
- **UIと通知機能**:
  - レスポンシブ対応のWebギャラリーや管理ダッシュボード機能。
  - SMTPを利用し、新規ターゲット検出時に指定されたメールアドレス（`RECIPIENT_EMAIL`）へアラートとサマリーを即時送信します。

### 通信方式
- **受信用**: インターネット向けに HTTPS (または HTTP) でエンドポイントを公開。エッジサーバからの HTTP POST リクエストを受け付け、`X-API-KEY` ヘッダーによるセキュアな認証を行います。

---
### アーキテクチャの重要指針（ルール準拠）
- **機能分離の原則**: エッジサーバで一次処理（YOLO推論やフィルタリング）を行い、クラウドサーバで集約・詳細分析・通知を行うことで、処理の偏りとネットワーク帯域の浪費を防いでいます。
- **インターフェースの整合性**: エッジサーバ（`pi` / `satos`）からの転送仕様（APIキーヘッダー、`Cycle ID` を含むファイル名フォーマット、多重送信時の挙動）は、クラウドサーバの受信パース仕様（正規表現など）に厳密に適合するよう一貫性を持たせています。
